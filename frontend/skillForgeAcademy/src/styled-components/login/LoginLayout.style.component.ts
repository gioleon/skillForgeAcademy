import { styled } from "@mui/material/styles"


export const LoginLayout = styled('div')(({theme}) => ({

    // display: 'flex',
    // flexDirection: 'row',
    // justifyContent: 'center',
    // alignItems: 'center',
    // height: '100vh',

}));