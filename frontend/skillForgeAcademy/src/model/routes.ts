
export enum PublicRoutes {
    HOME = "home",
    LOGIN = "login",
    REGISTER = "register",
    VERIFICATION = "verification",
    SUCCESSFUL = "successful"
}

export enum PrivateRoutes {
    PRIVATE = "private",
    PROFILE = "profile"
}